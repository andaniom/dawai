version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/queries.sql"
    schema: "migrations"
    gen:
      go:
        out: "internal/db"
        package: "db"
        sql_package: "pgx/v5"
